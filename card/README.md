# /card

Generated. Do not hand-edit these files.

Source and build live in the BrewInsight working repo at
`projects/brewinsight-business-card/digital/` (`build.py` + `card.template.html`).
Change the contact details or the card URL there, rebuild, and copy the output
back into this folder.

The card is intentionally outside the pre-launch gate: it does not load
`gate.js`, so it stays reachable while the rest of the site is curtained.
